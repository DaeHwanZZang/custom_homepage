# Custom Homepage 프로젝트

## 프로젝트 목표

Safari 브라우저의 **홈페이지(시작 페이지)로 지정할 수 있는 단일 HTML 파일**을 만든다.

- 로컬 파일(`file://`)로 열리는 것을 전제로 하며, 별도의 서버나 빌드 과정 없이 동작해야 한다.
- Safari에서 정상 동작하는 것이 최우선 기준이다 (다른 브라우저 호환은 부차적).

## 핵심 요구사항

### 1. 바로가기 버튼 (Quick Links)
- 주요 웹사이트로 한 번의 클릭으로 이동할 수 있는 바로가기 버튼 제공
- 자주 쓰는 사이트를 사용자가 쉽게 추가/수정/삭제할 수 있으면 좋음
- 사이트별 아이콘 또는 파비콘 표시로 시각적으로 구분

### 2. 히스토리 (방문 기록)
- 최근 방문한 사이트를 간략하게 목록으로 보여줌
- 항목을 클릭하면 해당 사이트로 바로 이동
- 참고 제약: 브라우저 보안 정책상 HTML/JS는 Safari의 실제 방문 기록에 접근할 수 없음
  → 이 페이지에서 클릭해 이동한 기록을 `localStorage`에 저장하는 방식 등으로 구현 필요
  → `file://` 환경에서 localStorage 동작 여부를 Safari에서 확인할 것

### 3. 추가 유용 기능 (선택 구현)
아래 중 유용한 것들을 선별해서 추가:
- 검색창 (Google 등 검색 엔진으로 바로 검색)
- 시계 / 날짜 표시
- 간단한 메모 또는 할 일 목록 (localStorage 저장)
- 다크 모드 / 라이트 모드 (시스템 설정 연동 `prefers-color-scheme`)
- 날씨 표시 (외부 API 필요 시 CORS 제약 확인)
- 즐겨찾기 그룹/카테고리 분류

## 기술 제약 및 방침

- **단일 HTML 파일**: HTML/CSS/JS를 하나의 파일에 인라인으로 포함 (외부 파일 의존 최소화)
- **로컬 실행**: `file://` 프로토콜에서 동작해야 하므로 CORS, localStorage 등 제약 사항을 Safari 기준으로 검증
- **데이터 저장**: 사용자 설정(바로가기 목록, 메모 등)은 `localStorage` 사용
- **외부 의존성 최소화**: CDN 라이브러리 사용은 오프라인 시 동작을 고려하여 신중하게 결정

## 구현 현황 및 확정된 결정 사항

- UI 텍스트는 **전부 영어** 사용
- 스타일은 **Anthropic Claude 스타일** (아이보리/다크 차콜 배경, 코랄 `#d97757` 포인트, 세리프 디스플레이)
- 바로가기 아이콘은 Google 파비콘 서비스(`google.com/s2/favicons`)로 실제 사이트 아이콘 표시,
  로드 실패(오프라인 등) 시 이니셜 모노그램 타일로 대체 — 외부 의존성 최소화 원칙의 예외로 허용됨
- 검색창은 페이지 로드 시 자동 포커스되며, 입력 시 Google Suggest(`suggestqueries.google.com`)를
  이용한 드롭다운 자동완성을 제공 (오프라인/CORS 차단 시 조용히 비활성화, 기능에 영향 없음)
- 바로가기 추가 팝업(`<dialog>`)은 뷰포트 중앙에 고정 표시

### 히스토리 (방문 기록) — 최초엔 제외했다가 이후 명시적 요청으로 추가됨
- 초기 요구사항 정의 단계에서는 "구현하지 않기"로 결정했으나, 구현 완료 후 사용자가 별도로
  히스토리 페이지를 명시적으로 요청하여 추가함
- 단일 파일 원칙의 예외로 `history.html`을 별도 페이지로 분리 (홈 화면 우측 상단 버튼으로 이동)
- 기록 대상: 바로가기 타일 클릭, 검색창에 직접 주소를 입력해 이동한 경우만 저장
  (일반 검색어 입력 후 Google 검색으로 이동하는 것은 "사이트 방문"이 아니므로 기록하지 않음 — 프라이버시 고려)
- `localStorage`의 `hp.history`에 최대 50건 저장, 페이지 내 "Clear history" 버튼으로 전체 삭제 가능

## Safari 홈페이지 설정 방법 (참고)

Safari > 설정(Settings) > 일반(General) > 홈페이지(Homepage)에 `file:///경로/index.html` 지정,
"새 윈도우/탭 열 때" 항목을 "홈페이지"로 설정.
